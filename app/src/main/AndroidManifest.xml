<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:name=".App"
        android:allowBackup="false"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher_r"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_r_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.NoteDemo"
        tools:targetApi="31">
        <activity
            android:name=".presentation.ui.recyclebin.RecycleActivity"
            android:exported="false" />
        <activity
            android:name=".presentation.ui.update.UpdateActivity"
            android:exported="false" />
        <activity
            android:name=".presentation.ui.home.HomeActivity"
            android:exported="false" />
        <activity
            android:name=".presentation.ui.splash.SplashActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".presentation.ui.adddata.MainActivity"
            android:exported="false" />
    </application>

</manifest>